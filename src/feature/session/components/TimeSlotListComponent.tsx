import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const TimeSlotListComponent = (props: Props) => {
  return (
    <View>
      <Text>TimeSlotListComponent</Text>
    </View>
  );
};

export default TimeSlotListComponent;

const styles = StyleSheet.create({});
