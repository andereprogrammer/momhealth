import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const SessionMainScreen = (props: Props) => {
  return (
    <View>
      <Text>SessionMainScreen</Text>
    </View>
  );
};

export default SessionMainScreen;

const styles = StyleSheet.create({});
