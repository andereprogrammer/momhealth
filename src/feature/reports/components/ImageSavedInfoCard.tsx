import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const ImageSavedInfoCard = (props: Props) => {
  return (
    <View>
      <Text>ImageSavedInfoCard</Text>
    </View>
  );
};

export default ImageSavedInfoCard;

const styles = StyleSheet.create({});
