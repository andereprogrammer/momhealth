import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const PackageShowCaseComponent = (props: Props) => {
  return (
    <View>
      <Text>PackageShowCaseComponent</Text>
    </View>
  );
};

export default PackageShowCaseComponent;

const styles = StyleSheet.create({});
